# 🍺 1708 — Система управления заказами бара

Real-time система для бара: приём заказов, управление очередью, учёт склада и гостевое меню по QR-коду.

---

## Структура проекта

```
bar-kviz-dubna/
├── index.html          # Основное приложение (бармен / официант / менеджер)
├── guest.html          # Гостевое меню — открывается по QR-коду
├── style.css           # Стили основного приложения
├── manifest.json       # PWA-манифест (иконки, название, цвета)
├── sw.js               # Service Worker (push-уведомления, офлайн-кэш)
│
├── icons/              # Иконки приложения
│   ├── icon-16.png
│   ├── icon-32.png
│   ├── icon-192.png
│   ├── icon-512.png
│   ├── apple-touch-icon.png
│   └── logo-transp.png
│
└── js/                 # JavaScript-модули
    ├── main.js         # Точка входа: Firebase-слушатели, обработчик кликов, boot
    ├── state.js        # Глобальное состояние приложения (orders, role, tablesMeta и др.)
    ├── firebase.js     # Инициализация Firebase, экспорт db/auth/helpers
    ├── utils.js        # Утилиты: форматтеры, парсер позиций, confirm-модал, flash
    ├── menu-data.js    # Встроенное меню (категории и позиции) — фолбэк если Firebase пуст
    ├── notifications.js# Service Worker, звук, push-уведомления о новых заказах
    ├── stock.js        # Списание и возврат остатков склада при заказах
    ├── tables.js       # Управление столами, рендеринг вкладок "Столики" и "Закрытые"
    ├── orders.js       # CRUD заказов, действия с позициями, модал редактирования
    ├── menu.js         # Пикер позиций (для официанта), редактор меню, рендеринг страницы меню
    ├── render.js       # Рендеринг очереди, карточек заказов, строк позиций, статистики
    ├── ui.js           # Роль, пароль, навигация по вкладкам, боковое меню
    ├── quiz.js         # Подготовка и завершение квиза (генерация QR для всех столов)
    └── calls.js        # Вызовы официанта от гостей
```

---

## Роли

| Роль | Что видит |
|------|-----------|
| **Бармен** | Очередь заказов, кнопки «Начал» / «Готово» по каждой позиции |
| **Официант** | Новый заказ, список готовых к выдаче, кнопка «Отнёс», вызовы от гостей |
| **Менеджер** | Всё вышеперечисленное + статистика, редактор меню, управление столами |

---

## Основные функции

- **Real-time синхронизация** — заказы обновляются мгновенно на всех устройствах через Firebase
- **Гостевое меню** — гость сканирует QR, выбирает позиции и отправляет заказ
- **Учёт склада** — остатки списываются при заказе и возвращаются при удалении или редактировании
- **Мгновенная доставка** — пиво, напитки и закуски официант может сразу отметить как доставленные
- **Push-уведомления** — бармен получает звуковой сигнал и уведомление при новом заказе
- **Вызов официанта** — гость вызывает официанта кнопкой из гостевого меню
- **Пробковый сбор** — добавляется к счёту стола одним нажатием (100 / 700 / 1000 ₽)
- **Добавки к чаю** — чабрец, лимон, мята (+50₽) и общий счётчик кружек на стол
- **Опции позиций** — к любой позиции меню можно задать выбор (например: с лаймом / без)
- **Квиз** — генерация QR-кодов для всех столов для печати (режим квиз-бара)
- **Сброс счётчика заказов** — номера заказов можно обнулить вручную (только менеджер)
- **PWA** — устанавливается на телефон как приложение
- **Офлайн-защита** — при потере интернета показывается предупреждение, ошибки записи не теряются молча

---

## Стек

- **Firebase Realtime Database** — хранение заказов, меню, столов
- **Firebase Auth** — анонимная авторизация
- **Vanilla JS (ES Modules)** — без фреймворков и сборщиков
- **PWA** — Service Worker, Web App Manifest
